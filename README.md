# Playfab Spammer
Simple PlayFab spammer. Probably the fastest out there. It uses secret key so it can't be used agaist the others.

# How to use?
You will have to install requests so it will work.
pip install requests

# Made by MamaMonke
