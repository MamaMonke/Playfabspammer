# Playfab Spammer
Simple PlayFab spammer. Probably the fastest out there. It uses secret key so it can't be used agaist the others.

# How to use?
You will have to install requests so it will work.
pip install requests

# A little about the spammer
Its made on python so its kinda slow. It uses secret key and bypasses player creation, so even if you disable
Disable player creation using Client/LoginWithCustomId
Disable player creation using Client/LoginWithAndroidDeviceId
Disable player creation using Client/LoginWithIOSDeviceId
Disable player creation using Client/LoginWithNintendoSwitchDeviceId

Its also the fastest spammer out there.

# Made by MamaMonke
